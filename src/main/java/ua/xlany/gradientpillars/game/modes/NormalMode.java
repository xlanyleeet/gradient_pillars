package ua.xlany.gradientpillars.game.modes;

import ua.xlany.gradientpillars.models.Game;

public class NormalMode implements GameModeHandler {

    @Override
    public void apply(Game game) {
        // Normal mode functionality (default 20 HP is handled by GameManager globally)
    }
}
