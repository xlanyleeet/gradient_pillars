# Список змін - GradientPillars v2.0.0 (Оновлення сесії)

## 🛠 Технічні покращення

### Система баз даних

- **Підтримка SQLite**: Додано повну підтримку SQLite (локальна база даних у файлі) як основного методу зберігання.
- **Розумне завантаження бібліотек**:
  - Драйвери для SQLite та MariaDB більше не вбудовуються в JAR плагіна (розмір зменшено з ~20MB до ~150KB).
  - Реалізовано `LibraryUtil`, який автоматично завантажує необхідні драйвери в папку `plugins/GradientPillars/libs/` під час першого запуску.
  - Виправлено проблеми з доступом до модулів Java (Java 16+) за допомогою кастомного завантажувача класів.
- **Стабільность**: Виправлено падіння ігрового циклу через помилки SQL, додано покращену обробку помилок.

### Конфігурація

- **Авто-міграція**: Файли конфігурації (`config.yml`, `items.yml`, `messages_*.yml`) тепер автоматично оновлюються при оновленні плагіна.
  - Відсутні ключі автоматично додаються з внутрішніх ресурсів JAR, не перезаписуючи налаштування користувача.
  - Логіку міграції винесено в окремий утилітний клас `ConfigUtil`.

## 🎮 Ігрові зміни

### Режим глядача

- **Миттєве відродження**: Глядачі тепер пропускають екран "Ви загинули" завдяки `GameRule.DO_IMMEDIATE_RESPAWN`.
- **Досвід**: Більш плавний перехід від гравця до глядача.

### Команди

- **Команда статистики**: Додано `/gp stats [player]` для перегляду перемог, поразок та відсотка перемог.
- **Підтримка консолі**: Додано можливість виконання `/gp reload` з консолі сервера.

### Предмети та чат

- **Зменшення спаму**: Видалено повідомлення про отримання кожного окремого предмета.
- **Локалізація**: Додано український переклад для статистики та інших нових функцій.

## 📝 Зміни у файлах

- **Змінено**: `pom.xml`, `paper-plugin.yml`, `GradientPillars.java`, `ItemManager.java`, `GameManager.java`, `ConfigManager.java`, `ItemsConfigManager.java`, `messages_uk.yml`.
- **Створено**: `LibraryUtil.java`, `StatsCommand.java`, `ConfigUtil.java`.
